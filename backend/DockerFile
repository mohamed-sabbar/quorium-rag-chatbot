# Utilise une image de base Python légère
FROM python:3.11-slim

# Définit le répertoire de travail dans le conteneur
WORKDIR /app

# Copie le fichier de dépendances
COPY requirements.txt .

# Installe les dépendances Python
# L'option --no-cache-dir réduit la taille de l'image
RUN pip install --no-cache-dir -r requirements.txt

# Copie le reste du code de l'application (ingest.py, main.py, etc.)
COPY . .

# Expose le port par défaut de FastAPI (8000)
EXPOSE 8000

# Commande par défaut pour démarrer le serveur (sera écrasée par docker-compose)
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]